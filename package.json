{
  "private": true,
  "workspaces": [
    "app",
    "wasm-bridge/pkg"
  ],
  "scripts": {
    "wasm": "cd wasm-bridge && wasm-pack build --target web",
    "build": "npm run build --workspace=app"
  }
}
